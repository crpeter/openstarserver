# Morphology-grid test ownership

Tests in this directory cover only the matching package at
`openstar_workloads/plugins/morphology_grid/`.

- Keep all morphology-grid tests and fixtures in this directory.
- Do not edit shared platform tests, sibling workload tests, coordinator code,
  or shared workload contracts.
- Preserve exact published wire IDs and strict nested-schema behavior.
- Exercise coordinator integration through public runtime interfaces.
